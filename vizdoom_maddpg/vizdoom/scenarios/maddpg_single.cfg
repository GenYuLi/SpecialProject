doom_scenario_path = maddpg_single.wad

# Rewards
living_reward = 0.0005

# Rendering options
screen_resolution = RES_160X120
screen_format = RGB24
render_hud = true
render_crosshair = false
render_weapon = true
render_decals = false
render_particles = false
window_visible = true

# Episode options
episode_start_time = 14
episode_timeout = 10000

# Available buttons
available_buttons = 
	{ 
		ATTACK 

        MOVE_RIGHT 
        MOVE_LEFT 
        MOVE_BACKWARD
        MOVE_FORWARD
		
        TURN_RIGHT 
        TURN_LEFT 
	}

# Game variables that will be in the state
available_game_variables = 
	{ 		
        KILLCOUNT
        HEALTH
        ARMOR
        SELECTED_WEAPON
        SELECTED_WEAPON_AMMO
    }
	
# Game mode
mode = PLAYER
